package com.nyora.hasan72341.mihon.parsers.webview

/**
 * Re-exports of the upstream parser-infrastructure webview types under the
 * native shim package. The lib type is named `InterceptionConfig`; callers that
 * import it `as ParsersInterceptionConfig` resolve through this alias.
 */
typealias InterceptedRequest = org.koitharu.kotatsu.parsers.webview.InterceptedRequest
typealias InterceptionConfig = org.koitharu.kotatsu.parsers.webview.InterceptionConfig
